package Test3;

message Test3Mes{ // ptoto2
		required int32 age = 1;// 1到15这期间的数字只会占用1个字节，超过15占用2个字节
		optional string ID = 2;  // required 唯一的 repeated 可重复的 optional 可选的
		repeated Goods goods = 3;
}

message Goods{
	required string ID = 1;
	required int32 price = 2;
}